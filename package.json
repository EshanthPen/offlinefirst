{
  "name": "offlinefirst",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first educational platform — SDGs 4, 9, 10",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install --omit=dev",
    "start": "node server/index.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
